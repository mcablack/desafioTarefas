# Projeto Tarefas 

## Laravel 8 (API) + Vue.js 2 (frontend) + Docker Compose.

## Como rodar (passos iniciais)

## 1. Copie ".env.example" para "backend/.env" e ajuste se necessário.
## 2. Execute:  docker-compose up --build -d